<ui:FluentWindow x:Class="FleetAutomate.View.Dialog.ActionConfigurationDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Configure Action"
                 Height="620"
                 Width="680"
                 MinHeight="360"
                 MinWidth="520"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="CanResize"
                 WindowStyle="None">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="HeaderTextBlock" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock x:Name="SubHeaderTextBlock" FontSize="12" Foreground="Gray" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="FieldsPanel"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button Content="OK"
                       Appearance="Primary"
                       Click="OkButton_Click"
                       Margin="0,0,10,0"
                       MinWidth="88"/>
            <ui:Button Content="Cancel"
                       Click="CancelButton_Click"
                       MinWidth="88"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
